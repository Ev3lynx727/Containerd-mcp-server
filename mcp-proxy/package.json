{
  "name": "mcp-stdio-proxy",
  "version": "1.0.0",
  "description": "Proxy to bridge STDIO MCP servers to HTTP",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "body-parser": "^1.20.0"
  }
}